{
  "name": "outputty",
  "version": "0.6.1",
  "private": true,
  "description": "Dev tooling for the outputty Claude Code plugin (prettier).",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.4"
  }
}
