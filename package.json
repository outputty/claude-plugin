{
  "name": "outputty",
  "version": "0.6.1",
  "private": true,
  "description": "Dev tooling for the outputty Claude Code plugin (prettier + oxlint).",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:file": "prettier --write",
    "lint": "oxlint",
    "lint:file": "oxlint",
    "test": "bun skills/outputty/tasks.test.js && bun skills/outputty/docs.test.js",
    "driver": "node .claude/skills/run-outputty/driver.mjs"
  },
  "devDependencies": {
    "oxlint": "^1.73.0",
    "prettier": "^3.9.4"
  }
}
