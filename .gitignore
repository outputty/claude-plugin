# Local reference / scratch drop — not plugin content
prod/

# macOS archive artifacts
__MACOSX/
._*
.DS_Store

# Node dev tooling (prettier + oxlint) — pnpm-lock.yaml is committed, node_modules is not
node_modules/

# OpenWolf operational memory — machine-local, never committed (its own hooks own it)
.wolf/
