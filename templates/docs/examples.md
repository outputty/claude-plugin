# Examples

The canonical base program and its data, reused verbatim in replies, tickets, PRs and docs. Each block runs; the docs layer re-runs a block whose output changed.

## <example name>

```lang
<the call> // <real output>
```
