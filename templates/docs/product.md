# Product

Read first, every session. Written by `/plan` when a decision settles; pruned when a line no longer holds. `init` drafts it from the README and the code and settles every line with the user.

## North Star

<!-- One paragraph. What this product is for, who it is for, and the one thing it must never become. A sentence true of a dozen other projects is cut. Every claim traces to README, docs or code. -->

## Language

<!-- One canonical term per line: the term, a one-line definition, the synonyms it replaces. Only terms the code or the docs actually use; a term with no synonym to replace is not listed. -->

- **<term>** - <definition>. (replaces: <synonyms>)
