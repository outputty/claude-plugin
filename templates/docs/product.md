# Product

Read first, every session. Written by `/grill` when a decision settles; pruned when a line no longer holds.

## North Star

<one paragraph: what this product is for, and the one thing it must never become>

## Language

<one canonical term per line: the term, a one-line definition, the synonyms it replaces>

- **<term>** - <definition>. (replaces: <synonyms>)
